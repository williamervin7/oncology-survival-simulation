# GitHub Actions Workflows

Placeholder README for automated CI/CD and statistical validation workflows.

## Planned Workflows

*   **`ci.yml`**: Automated linting, code formatting, and unit testing (`pytest tests/software/`) on push and pull requests.
*   **`statistical_validation.yml`**: Automated verification of mathematical and statistical invariants (`pytest tests/statistical/`), ensuring probability matrices and model estimates remain valid across build changes.